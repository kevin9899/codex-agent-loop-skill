interface:
  display_name: "Loop"
  short_description: "증거, 검증, 5-agent gate로 끝까지 밀고 가는 $loop."
  default_prompt: "Use $agent-loop to keep implementation work moving to the real final goal, treat the operator token itself as permission to use delegated spawn_agent lanes, record capability_mode with delegated_agents_authorized_by_loop plus a tool-availability suffix, keep status briefings live-state only, and require fresh viewpoint-separated 5-agent halt/completion proof before any terminal closeout."
